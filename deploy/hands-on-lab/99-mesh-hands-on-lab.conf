# The local Kubernetes workload already consumes more than the distribution
# default of 128 root-owned inotify instances. Persistent systemd lab
# containers need headroom to boot and restart without EMFILE.
fs.inotify.max_user_instances = 512

