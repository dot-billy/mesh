apiVersion: v1
kind: Service
metadata:
  name: {{ include "mesh-release-origin.fullname" . }}
  labels:
    {{- include "mesh-release-origin.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "mesh-release-origin.selectorLabels" . | nindent 4 }}
  ports:
    - name: https
      port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
